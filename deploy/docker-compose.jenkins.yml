services:
  web:
    image: steel_web:${IMAGE_TAG}
    container_name: steel_web
    ports:
      - "5002:5000"
    env_file:
      - ../env
    environment:
      DATABASE_URL: postgresql+psycopg2://diana:20041902@host.docker.internal:5540/db
    restart: unless-stopped

